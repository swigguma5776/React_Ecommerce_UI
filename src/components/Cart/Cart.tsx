import * as React  from 'react'; 

// internal imports
import { NavBar } from '../sharedComponents';




export const Cart = () => {
    return (
        <div>
            <NavBar />
            <h1 style={{marginTop: '100px', marginLeft: '100px'}}>Cart</h1>
        </div>
    )
}